# Generated by fzctl onboard — DO NOT COMMIT (gitignored by `.env.*`).
# Source of truth for one tenant instance. Mode 0600.
FAZTRACK_ENV=production
FAZTRACK_DATABASE_URL=postgresql+psycopg://{{DB_USER}}:{{DB_PASSWORD}}@localhost:{{DB_PORT}}/{{DB_NAME}}
FAZTRACK_JWT_SECRET={{JWT_SECRET}}
FAZTRACK_CORS_ORIGINS={{CORS_ORIGINS}}
FAZTRACK_DEMO_SEED_PASSWORD={{SEED_PASSWORD}}
FAZTRACK_DEMO_WORKER_PIN={{WORKER_PIN}}
FAZTRACK_ACCESS_TOKEN_MINUTES=30
